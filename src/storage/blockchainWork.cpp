#include "blockchainWork.h"
#include "crypto_utils.h"

void addTotalWork(const Array256_t& difficulty)
{
    // blockWork = uint256MAX / (difficulty + 1)

    // Add block work to total
}

void removeTotalWork(const Array256_t& difficulty)
{
    // blockWork = uint256MAX / (difficulty + 1)

    // remove block work to total
}

Array512_t getTotalWork()
{
    return {};
}



